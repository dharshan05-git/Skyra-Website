import User from '../../models/User.js'; import ApiError from '../../utils/ApiError.js'; import { success } from '../../utils/ApiResponse.js'; import { escapeRegex } from '../../utils/helpers.js';
export const listUsers = async (req, res) => { const page=Math.max(1,Number(req.query.page)||1), limit=Math.min(100,Math.max(1,Number(req.query.limit)||20)); const search=escapeRegex(String(req.query.search||'').slice(0,100)); const filter=search?{$or:[{name:{$regex:search,$options:'i'}},{email:{$regex:search,$options:'i'}}]}:{}; const [users,total]=await Promise.all([User.find(filter).sort({createdAt:-1}).skip((page-1)*limit).limit(limit).lean(),User.countDocuments(filter)]); const onlineAfter=Date.now()-90*1000; const withPresence=users.map((user)=>({...user,online:Boolean(user.lastSeenAt&&new Date(user.lastSeenAt).getTime()>=onlineAfter)})); return success(res,{users:withPresence,pagination:{page,limit,total,pages:Math.ceil(total/limit)}}); };
export const updateUser = async (req,res)=>{ const user=await User.findByIdAndUpdate(req.params.id,{active:req.body.active},{new:true,runValidators:true}); if(!user) throw new ApiError(404,'User not found'); return success(res,{user},'User updated'); };
