import { Router } from 'express'; import { createPaymentOrder, verify } from '../controllers/payment.controller.js'; import { authenticate } from '../middleware/auth.middleware.js'; import { validate } from '../middleware/validate.middleware.js'; import { asyncHandler } from '../utils/asyncHandler.js'; import { orderIdSchema, paymentVerificationSchema } from '../validators/order.validator.js';
const router = Router(); router.use(authenticate); router.post('/orders/:id', validate(orderIdSchema), asyncHandler(createPaymentOrder)); router.post('/verify', validate(paymentVerificationSchema), asyncHandler(verify)); export default router;
